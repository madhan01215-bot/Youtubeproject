#include <stdio.h>

int main() {
    int a = 5, b = 3;
    printf("Sum = %d", a + b);
    return 0;
}
#include <stdio.h>

int main() {
    int a = 10, b = 4;
    printf("Subtraction = %d", a - b);
    return 0;
}